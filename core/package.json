{
  "name": "payroll-os-core",
  "version": "0.0.1",
  "private": true,
  "description": "payroll-os 오케스트레이션 로직 (GUI 독립)",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.12.0",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
